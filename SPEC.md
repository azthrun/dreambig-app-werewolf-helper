# 狼人杀法官助手 — 技术规格说明书

> 版本 1.0 · 面向手机竖屏的静态网页应用 · 部署于 GitHub Pages

---

## 1. 概述

### 1.1 产品定位

一个供**线下狼人杀法官（上帝）**在带场时使用的手机网页工具。法官单人持有一台手机，用它配置局型、引导夜晚流程、推算天亮死亡、追踪技能消耗、计时与记录日志。

### 1.2 核心设计哲学：辅助型，而非裁决型

应用**提示但绝不阻断**。

- 引擎会推算天亮死亡名单、计算查验结果、提示技能触发时机 —— 但这些一律是**建议**。
- 法官可以覆盖任何结论：增删死亡条目、修改身份、跳过任何步骤。
- 应用永远不会拒绝一个操作，也不会因为"规则不允许"而禁用按钮。

**理由**：狼人杀的房规因桌而异（女巫能否首夜自救？同守同救是否死亡？被毒的猎人能否开枪？）。一个硬编码单一规则集的"裁决型"应用会在大量牌桌上给出错误结论，并与法官对抗。辅助型设计使应用永远有用、永远不会错。

真正会改变**死亡结果**的少数规则分歧，被提炼为 5 个设置开关（§7）；其余分歧交由法官人工裁定。

### 1.3 非目标（v1 明确不做）

| 不做 | 理由 |
|---|---|
| 警长竞选 / 警徽流 / 1.5票 | 增加大量白天子流程，违背"简易"原则。架构（触发队列 + 卡片徽标）已预留，v2 可无重构接入 |
| 投票计票（谁投了谁） | 12人局逐个点选耗时过长，会拖慢全场最紧张的环节。仅记录放逐结果 |
| 私密查看身份（传手机看牌） | 与实体牌竞争力弱，且需全屏锁定界面，与"无弹窗"原则冲突。数据模型已支持 v2 接入 |
| 多局历史归档 | 需求为"局内日志"，非跨局归档。单槽存档 + 日志复制导出已覆盖 |
| 联机 / 多设备同步 | 无后端。纯本地应用 |
| 屏幕阅读器完整支持 | 提供基础 `aria-label` 与焦点环，但不做完整无障碍适配 |
| 国际化 | 仅简体中文 |

---

## 2. 技术架构

### 2.1 技术选型

- **纯静态**：无框架、无构建步骤、无 npm 依赖、无后端、无外部网络请求。
- **原生 Web**：HTML + CSS + 原生 ES6+ JavaScript（ES Modules）。
- **部署**：`git push` 后由 GitHub Pages 直接托管源文件。

> **不采用**单文件方案。原型的单文件形态源自 Claude Design 工具的约束，并非 GitHub Pages 的要求。多文件更易审阅与维护，静态程度完全相同。

### 2.2 文件结构

```
/
├── index.html          # 唯一页面骨架
├── style.css           # 设计令牌 + 组件层 + 布局
├── app.js              # 应用入口、状态容器、渲染、事件绑定
├── engine.js           # 纯函数：天亮结算、触发链、查验结果计算
├── roles.js            # 19 个角色的声明式数据表 + 夜晚步骤元数据
├── storage.js          # localStorage 读写、版本迁移、姓名池
├── icons.svg           # 内联 SVG sprite（<symbol> 定义）
├── manifest.json       # PWA 清单
├── sw.js               # Service Worker
├── test.html           # engine.js 的断言测试页
├── SPEC.md             # 本文档
└── README.md           # 使用说明与部署说明
```

### 2.3 GitHub Pages 部署约束

- **必须使用相对路径**（`./app.js`、`./sw.js`）。若托管于项目站点子路径 `username.github.io/repo/`，绝对路径会导致 Service Worker 注册失败。
- `manifest.json` 的 `start_url` 与 `scope` 必须为 `./`。
- GitHub Pages 对静态资源返回 `Cache-Control: max-age=600`，重新部署后最长约 10 分钟传播。Service Worker 的版本化缓存策略（§13）必须能容忍此窗口。
- 仓库须为 **public**（private 仓库的 Pages 需付费计划）。
- `*.github.io` 默认 HTTPS，满足 Service Worker 的安全上下文要求。

### 2.4 目标浏览器

| 平台 | 最低版本 | 说明 |
|---|---|---|
| iOS Safari | 16.4+ | Screen Wake Lock API 自 16.4 起可用 |
| Android Chrome / Edge | 最新两个大版本 | |
| 桌面浏览器 | 现代版本 | 开发调试用，非目标使用场景 |

不提供 polyfill，不支持旧版浏览器。

---

## 3. 数据模型

### 3.1 角色定义（`roles.js`）

共 **19 个角色**。`情侣` 不是角色，而是状态（§5.3）。

```js
{
  id: 'seer',
  name: '预言家',
  camp: 'god',           // 'wolf' | 'god' | 'civ'
  icon: 'icon-seer',     // sprite 中的 symbol id
  nightStep: 'seer',     // 对应的夜晚步骤 id，null 表示无夜间行动
  skills: ['check'],     // 技能槽定义，见 §6
}
```

| 阵营 | 角色 | id | 夜晚步骤 | 死亡触发 | 白天主动 |
|---|---|---|---|---|---|
| 狼 | 普通狼人 | `wolf` | 狼人共刀 | — | 自爆 |
| 狼 | 狼王 | `wolfking` | 狼人共刀 | **开枪** | 自爆 |
| 狼 | 白狼王 | `whitewolfking` | 狼人共刀 | — | **自爆带人** |
| 狼 | 狼美人 | `wolfbeauty` | 狼人共刀 + **魅惑** | **魅惑者殉情** | 自爆 |
| 狼 | 隐狼 | `hiddenwolf` | 无（不与狼队睁眼） | — | — |
| 狼 | 机械狼 | `mechwolf` | **首夜复制身份** | 继承所复制角色 | 自爆 |
| 神 | 预言家 | `seer` | 查验阵营 | — | — |
| 神 | 女巫 | `witch` | 解药 / 毒药 | — | — |
| 神 | 猎人 | `hunter` | 无 | **开枪** | — |
| 神 | 守卫 | `guard` | 守护 | — | — |
| 神 | 白痴 | `idiot` | 无 | **翻牌免死** | — |
| 神 | 骑士 | `knight` | 无 | — | **决斗** |
| 神 | 丘比特 | `cupid` | 首夜连线 | — | — |
| 神 | 魔术师 | `magician` | 交换指向 | — | — |
| 神 | 守墓人 | `gravekeeper` | 查验昨日被放逐者 | — | — |
| 神 | 狐狸 | `fox` | 查验三连 | — | — |
| 神 | 熊 | `bear` | **自动判定咆哮** | — | — |
| 神 | 通灵师 | `psychic` | 查验精确身份 | — | — |
| 民 | 平民 | `villager` | 无 | — | — |

### 3.2 玩家（Player）

```js
{
  seat: 3,                    // 座位号 1..N，固定不变
  name: '小明',                // 空字符串时显示「座位3」
  roleId: 'witch' | null,     // null = 未知身份（渐进填充，§8.4）
  effectiveRoleId: 'witch',   // 机械狼复制后的有效身份，通常等于 roleId
  alive: true,
  deathReason: null,          // §5.2 的死因枚举
  deathDay: null,             // 死于第几天/夜
  deathPhase: null,           // 'night' | 'day'
  loverSeat: null,            // 情侣链接（丘比特或手动配对）
  charmedBySeat: null,        // 被狼美人魅惑
  isSheriff: false,           // v1 恒为 false，为 v2 预留
  skills: { antidote: true, poison: true },  // 见 §6
  flags: {
    idiotRevealed: false,     // 白痴已翻牌
    foxDisabled: false,       // 狐狸技能已失效
  },
}
```

### 3.3 完整游戏状态（GameState）

```js
{
  version: 1,                 // 存档版本，不匹配即丢弃（§11.3）
  screen: 'setup1' | 'setup2' | 'setup3' | 'setup4' | 'game' | 'log' | 'report',
  playerCount: 12,
  roleCounts: { wolf: 4, seer: 1, ... },
  players: [ Player, ... ],
  nightOrder: ['cupid', 'magician', 'guard', ...],
  rules: { ... },             // §7 的 5 个开关
  settings: {
    soundEnabled: false,
    randomFirstSpeaker: true,
    theme: 'auto' | 'light' | 'dark',
  },
  day: 1,
  phase: 'night' | 'day',
  stepIndex: 0,
  nightActions: {             // 本夜已收集的行动，供结算引擎消费
    guardTarget: 5,
    wolfTarget: 3,
    witchAction: { type: 'save' | 'poison' | 'skip', target: 3 },
    magicianSwap: [2, 7],
    charmTarget: 9,
    ...
  },
  lastGuardTarget: null,      // 用于连守检查
  pendingDeaths: [],          // 结算引擎产出、待法官确认的死亡提案
  triggerQueue: [],           // 待处理的死亡触发（§5.4）
  timer: {
    mode: 'free' | 'speech',
    endsAt: 1723400000000,    // 截止时间戳，非倒计时整数
    pausedRemaining: null,    // 暂停时保存剩余毫秒
    running: false,
    speechSeat: null,
    speechDirection: 1,       // 1 = 顺时针，-1 = 逆时针
  },
  log: [ LogEntry, ... ],
  history: [ GameState, ... ], // 全量快照，深度 20
  alert: null,                // 当前常驻警告横幅
}
```

### 3.4 日志条目（LogEntry）

```js
{
  day: 2,
  phase: 'night',
  type: 'skill' | 'death' | 'system' | 'note' | 'warning',
  actor: 5,                   // 座位号，可为 null
  targets: [3],
  text: '预言家查验 3号 → 狼人',
  result: '狼人',             // 信息类步骤的答案
  ts: 1723400000000,
}
```

---

## 4. 游戏流程

### 4.1 设置阶段（4 步向导）

**Step 1 — 人数与角色牌**
- 人数 6–20，加减按钮调整。
- 预设：9人标准局（3狼 + 预女猎 + 3民）、12人标准局（4狼 + 预女猎守 + 4民）。
- 19 个角色按阵营分组（狼人 / 神职 / 平民），逐个加减数量。
- 顶部实时显示 `已选 X / 需 N 人`，数量不符时以标签样式提示（**不禁用**下一步 —— 辅助型原则）。
- 若存在未完成存档，顶部显示**内联横幅**：`检测到未完成的对局（第2晚）· 继续 / 放弃`。「放弃」需长按确认。

**Step 2 — 玩家名单**
- 每个座位一行：座位号 + 拖拽把手（⠿）+ 姓名输入框。
- **姓名选填**，留空时游戏内显示「座位N」。
- **沿用上次名单** 按钮（存在上次存档时显示），一键回填。
- 输入框聚焦时，下方展开**姓名池芯片**（最近使用优先，最多 50 个），点击即填入。
- **拖拽排序**：仅可从把手起拖，重排姓名在固定座位间的分配（座位号本身不变）。基于 Pointer Events 实现，支持触摸；拖动时卡片抬起、占位间隙动画、靠近边缘自动滚动。
- **清空已存姓名** 按钮（长按 600ms 确认）：清空姓名池与上次名单，不影响进行中的对局。

> 座位顺序具有**机制意义**：熊的咆哮判定依赖存活邻座，发言计时依座位顺序行走。必须与实际牌桌一致。

**Step 3 — 夜晚顺序与计时**
- 列出当前局型中**实际存在**的夜晚步骤，可拖拽排序（复用 Step 2 的拖拽组件）。
- **恢复默认顺序** 按钮。
- 默认顺序：`丘比特 → 魔术师 → 守卫 → 狼人 → 机械狼 → 女巫 → 预言家 → 通灵师 → 狐狸 → 守墓人 → 熊`
- 计时默认值：白天讨论（默认 180s）、夜晚行动（默认 60s）。
- **高级规则**（默认折叠）：§7 的 5 个开关 + 每日随机首发言开关 + 提示音开关。

**Step 4 — 分配身份**
- 紧凑网格：点座位 → 点角色。**不使用**下拉列表堆叠。
- **随机分配剩余身份** 按钮（使用 `crypto.getRandomValues`）。
- **身份分配非必填**。`开始游戏` 始终可点击。未分配的座位为「未知身份」。

### 4.2 局内 — 夜晚

1. 按 `nightOrder` 依次呈现步骤，每步显示：角色名、法官口播提示语、目标选择要求。
2. 法官点选目标座位（卡片高亮）。**若该座位身份未知，点选即隐式补全为该角色**（§8.4）。
3. 信息类步骤在选定目标后**直接显示答案**（§9）。
4. `完成` 提交步骤 / `跳过` 略过。
5. 全部步骤完成后，显示 `天亮了` 按钮 → 触发天亮结算（§5.1）。

### 4.3 局内 — 白天

天亮结算确认后进入白天，顺序为：

1. **死亡公布** — 引擎推算的死亡名单，法官可增删后确认。
2. **触发队列处理**（§5.4）—— 猎人开枪、狼王开枪等逐条提示。
3. **随机首发言抽取**（若开启）—— 内联横幅显示 `本轮发言：5号 开始 · 顺时针`，附 `重新抽取`。
4. **发言计时** — 可选启动，高亮当前发言者，`下一位` 自动跳至下一存活座位。
5. **白天主动行为** — 骑士决斗 / 白狼王自爆 / 普通狼人自爆，随时可发起。
6. **放逐** — 法官长按被放逐者卡片 → 选择死因 `被投票`。触发链自动处理（白痴翻牌、猎人开枪、情侣殉情）。
7. `进入下一夜` → day + 1，phase → night。

### 4.4 结束

- 顶部 `长按结束`（600ms，带进度反馈）→ 进入**战报页**。
- 战报页内容：胜方（法官点选 好人胜 / 狼人胜 / 平局）、完整名单（座位 / 姓名 / 身份 / 死因 / 死亡时间）、总时长、天数、完整日志。
- 操作：`复制日志文本`（写入剪贴板，可粘贴到微信群）、`开新局`（清空存档槽）、`返回`（对局继续，误触可恢复）。

---

## 5. 结算引擎（`engine.js`）

引擎为**纯函数**集合：输入 `GameState`，输出提案，不产生副作用。这是全项目最需要正确性保证的部分，因此可测试性优先于便利性。

### 5.1 天亮结算

```
resolveDawn(state) -> { deaths: [{seat, reason, explanation}], warnings: [...] }
```

处理顺序：

1. **魔术师交换** — 若本夜有 `magicianSwap: [A, B]`，将本夜所有以 A / B 为目标的行动指向互换。
2. **狼刀** — `wolfTarget` 标记为待死，原因 `被狼杀`。
3. **守卫守护** — 若 `guardTarget === wolfTarget`，撤销该死亡。
4. **女巫解药** — 若 `witchAction.type === 'save'` 且目标为待死者，撤销该死亡。
5. **同守同救判定** — 若守卫与解药作用于同一人：依 `rules.doubleProtectKills`（默认 **死亡**）决定结果，并推入 `warnings`。
6. **女巫毒药** — `witchAction.type === 'poison'` 的目标标记为待死，原因 `被毒`。
7. **情侣殉情** — 任一死亡者若有 `loverSeat` 且对方存活，对方标记为待死，原因 `殉情`。
8. **狼美人魅惑连锁** — 狼美人死亡时，`charmedBySeat` 指向者标记为待死，原因 `被魅惑`。
9. **递归收敛** — 步骤 7、8 产生的新死亡重新进入 7、8，直至无新增（防循环：已处理座位集合去重）。

**每条死亡携带 `explanation`**，例如 `3号 被狼杀（未被守护 · 未使用解药）`，以便法官核对而非盲信。

结算结果写入 `pendingDeaths`，界面呈现为可增删的列表，法官确认后才真正落库。

### 5.2 死因枚举

`被投票` · `被狼杀` · `被毒` · `被枪` · `被骑士处决` · `殉情` · `被魅惑` · `自爆` · `被自爆带走` · `其他`

### 5.3 情侣 = 状态，非身份

情侣不占用 `roleId`。一名情侣玩家**仍保有其真实身份**（狼人情侣 / 预言家情侣），其技能标签与死亡触发均由真实身份派生。

情侣关系的建立途径：
- 丘比特首夜连线步骤；
- 法官手动配对（设置阶段或局内，点选两名玩家 → `设为情侣`）。

两张卡片显示 ♥ 标记。

### 5.4 死亡触发队列

死亡确认后，引擎检查每位死者是否具备触发能力，将符合条件者推入 `triggerQueue`，界面以**内联横幅逐条提示**（一次一条）：

```
3号（猎人）可开枪 →  点选目标  /  跳过
```

**死因感知的资格判定**：

| 触发 | 拥有者 | 抑制条件 |
|---|---|---|
| 开枪 | 猎人、狼王 | 死因为 `被毒` 或 `殉情` 时，依 `rules.abnormalDeathBlocksShot`（默认 **抑制**）不入队 |
| 翻牌免死 | 白痴 | 仅死因为 `被投票` 且未翻过牌时触发；触发后不死亡，仅置 `idiotRevealed = true`，失去投票权 |
| 魅惑殉情 | 狼美人 | 无条件（已在 §5.1 步骤 8 处理，不入队） |

**不入队的白天主动行为**（非死亡触发）：
- **白狼王自爆带人** — 白天主动按钮，选择目标后双双死亡（`自爆` / `被自爆带走`）。白狼王正常死亡（被投/被杀）时**不获得任何触发**。
- **骑士决斗** — 白天主动按钮，点选骑士与对手，法官裁定胜负后产生死亡。
- **普通狼人自爆** — 白天主动按钮，该狼死亡（`自爆`），白天立即结束，直接进入夜晚。

**所有触发产生的死亡回流 §5.1 的步骤 7–9**，因此「猎人开枪 → 击中情侣 → 殉情 → 对方是狼王 → 狼王开枪」这类连锁由同一循环逐条呈现，法官无需在脑中追踪。

### 5.5 熊的咆哮判定

```
bearGrowls(state) -> true | false | 'unknown'
```

取熊左右两侧**最近的存活座位**（跨过死者），若任一为狼阵营则返回 `true`。若任一邻座身份未知，返回 `'unknown'` 并提示法官手动判断。

---

## 6. 技能状态追踪

每位玩家的 `skills` 对象记录可消耗技能的剩余状态：

| 角色 | 技能槽 | 说明 |
|---|---|---|
| 女巫 | `antidote`, `poison` | 两瓶药独立消耗 |
| 守卫 | `lastTarget` | 记录上一夜守护目标，用于连守检查 |
| 猎人 / 狼王 | `shot` | 开枪一次性 |
| 白狼王 | `selfDestruct` | 自爆一次性 |
| 骑士 | `duel` | 决斗一次性 |
| 丘比特 | `link` | 首夜一次性 |
| 魔术师 | `swap` | 记录使用次数 |
| 机械狼 | `copy` | 首夜一次性，记录复制目标 |
| 白痴 | `revealed` | 见 `flags.idiotRevealed` |
| 狐狸 | `active` | 见 `flags.foxDisabled` |

**行为规则**：
- **自动消耗** — 确认「女巫使用解药」即置 `antidote = false`，无需额外操作。
- **可手动修正** — 展开玩家卡片可切换任一技能状态（辅助型原则）。
- **驱动流程** — 技能全部耗尽的角色，其夜晚步骤自动跳过（女巫双药用尽、狐狸失效）。
- **界面呈现** — 卡片上以小标签显示，已用技能以删除线 + 降低不透明度表示：`解药 ✓ · 毒药 ~~✗~~`。

---

## 7. 规则开关

位于设置 Step 3 的「高级规则」折叠区，默认收起。仅收录**真正改变死亡结果**的分歧项。

| 开关 | 键名 | 默认 | 说明 |
|---|---|---|---|
| 同守同救结果 | `doubleProtectKills` | **死亡** | 守卫守护 + 女巫解药作用于同一人时，该玩家是否仍死亡 |
| 女巫自救 | `witchSelfSave` | **仅首夜** | `never` / `firstNightOnly` / `always` |
| 守卫连守 | `guardRepeatAllowed` | **禁止** | 禁止时不阻断操作，仅触发警告横幅 |
| 非常规死亡抑制开枪 | `abnormalDeathBlocksShot` | **抑制** | 猎人 / 狼王 死于 `被毒` 或 `殉情` 时能否开枪 |
| 女巫同夜双药 | `witchBothPotions` | **不可** | 同一夜能否同时使用解药与毒药 |

**明确不做开关、交由法官人工裁定的分歧**：魔术师交换语义细节、机械狼复制语义、隐狼可见性变体、狼美人魅惑变体、白狼王自爆时机限制、骑士决斗时机限制、白痴翻牌后的额外限制、屠边 / 屠城胜利条件。

> 理由：这些分歧多数关乎**给玩家的信息**（由法官口播，应用不参与），而非**谁会死**；且低频角色的变体组合会使结算逻辑爆炸。

---

## 8. 交互规范

### 8.1 「无弹窗」的精确定义

**弹窗 =** 覆盖页面或阻断交互的任何元素：模态对话框、遮罩背景、原生 `alert()` / `confirm()` / `prompt()`、全屏接管层。**一律禁止。**

**不属于弹窗、允许使用**：
- 流内内联展开（卡片长高显示死因芯片，网格重排）
- 顶部 / 步骤区内联横幅
- 瞬时非阻断状态条
- 整页替换式屏幕（日志页、战报页 —— 有明确返回路径）

### 8.2 破坏性操作的确认策略

**仅两种机制，不设第三种。**

**（一）长按守护** —— 用于破坏性且难以恢复的操作：
- `长按结束` 对局
- `放弃` 未完成存档
- `清空已存姓名`
- `清空角色配置`

统一 600ms，按钮上以填充进度条给出可见反馈，使误触不可能发生且手势可学习。

**（二）先执行 + 撤销条** —— 用于其余一切操作：
操作立即生效，底部出现约 5 秒的非阻断状态条：`已标记 3号 阵亡（被狼杀） · 撤销`。覆盖：标记阵亡、复活、随机分配、技能状态修正。

### 8.3 手势约定

| 手势 | 对象 | 行为 |
|---|---|---|
| 点击 | 存活卡片（夜晚步骤中） | 选为当前步骤目标 |
| 点击 | 存活卡片（无进行中步骤） | 展开卡片：身份、技能状态、设为情侣、修改身份 |
| **长按 550ms** | 存活卡片 | 内联展开死因芯片 → 点选即标记阵亡 |
| **点击** | 阵亡卡片（折叠态） | 内联展开：身份、死因、`恢复存活` 按钮 |

> **关键修正**：原型中「长按存活卡片 = 标记阵亡」与「长按阵亡卡片 = 复活」共用同一手势，相邻卡片上语义相反，是误操作温床。v1 将复活改为**点击展开后按钮触发**，与标记阵亡彻底分离。

### 8.4 身份的渐进填充

身份分配非必填，未分配座位显示为「未知身份」。

- 夜晚步骤中点选行动者时，**若该座位身份未知，则隐式补全为当前步骤对应的角色**。游戏进行本身即完成花名册填充。
- 任何座位的身份可随时通过点击卡片 → 修改身份 更正。
- 死亡触发、技能标签、查验结果仅对**已知身份**生效；未知时优雅降级为「未知身份 — 请手动判断」，而非给出错误结论。

### 8.5 撤销

- 每次状态变更前推入**全量 `GameState` 快照**（原型仅快照 `players` + `log`，导致撤销「进入下一夜」后状态不一致）。
- 深度 20，超出丢弃最早项。
- 撤销是「先执行 + 撤销条」策略的安全网，因此其可靠性是硬性要求。

---

## 9. 信息角色：直接给出答案

应用已知座位→身份，且刚刚记录了狼刀目标，因此可直接推算法官需要口播的信息。**这是本应用最高价值的功能**：它消除了最容易出错、最拖慢夜晚节奏的记忆任务。

| 步骤 | 显示内容 |
|---|---|
| 预言家 | 大字号 **好人** / **狼人**（隐狼按房规默认判为**好人/金水**） |
| 通灵师 | 精确身份名称 |
| 守墓人 | **自动识别**昨日被放逐者并显示其身份，无需点选目标 |
| 女巫 | 步骤头部显示 `今晚死亡：X号`，随后才呈现 解药 / 毒药 / 不使用 选项 |
| 狐狸 | 法官点选起点后，显示连续三名存活玩家中**是否存在狼人**；若不存在则自动置 `foxDisabled = true` |
| 熊 | **应咆哮** / **不咆哮**（§5.5） |

所有结果一并写入日志：`第2夜 · 预言家查验 5号 → 狼人`，使局内日志成为真正的复盘记录。

**信息泄露风险**：法官屏幕会以大字号显示敏感结果。缓解措施 —— 结果仅呈现于步骤横幅区（法官本就以身体遮挡的区域），且局内界面默认暗色主题。

---

## 10. 警报通道

### 10.1 平台事实

**`navigator.vibrate()` 在 iOS Safari 上不受支持**，Apple 从未实现 Vibration API，网页侧无任何变通方案。Android Chrome 支持，但需用户手势后触发，且手机静音 / 勿扰时被忽略。

因此原始需求第 8 条的「微震动」**无法在 iPhone 上兑现**。本规格如实记录此限制，不做虚假承诺。

### 10.2 实现方案：视觉优先

- **常驻内联警告横幅** —— 显示于步骤区上方，**不自动消失**，需法官点击 `知道了` 确认。（原型的 4 秒后自动清除会被错过。）
- **卡片高亮** —— 相关玩家卡片以强调色脉冲边框（`wjAlert` 动画）。
- **震动** —— 可用时调用 `navigator.vibrate([120, 60, 120])`，视为渐进增强，绝不依赖。
- **提示音** —— 设置项，**默认关闭**。WebAudio 短促蜂鸣。默认关闭的理由：牌桌上的声响会被玩家听见，可能泄露「刚刚发生了什么」。

### 10.3 触发场景

同一通道承载引擎检测到的所有值得注意的交互：

- 同守同救
- 守卫连守违规
- 女巫自救违反当前开关设置
- 猎人 / 狼王 因非常规死亡被抑制开枪
- 熊应当咆哮
- 计时归零

---

## 11. 持久化

### 11.1 对局存档（单槽）

- 键名 `wolf.game`。每次状态变更后写入完整 `GameState`（含 players、log、rules、timer、history）。体积约数 KB，无需防抖。
- **单槽**，不做多局库。
- **显式恢复**：加载时若存在未完成对局，Step 1 顶部显示内联横幅 `检测到未完成的对局（第2晚）· 继续 / 放弃`。不静默恢复 —— 避免法官开新局时被塞进上一局的陈旧状态。
- `长按结束` 并在战报页点击 `开新局` 后清空该槽。

> **必要性**：iOS Safari 会主动回收后台标签页。法官的手机在 40 分钟的对局中必然会被切走（回微信、锁屏、来电）。无持久化 = 整局数据不可恢复地丢失。

### 11.2 姓名池（独立生命周期）

- 键名 `wolf.names`：`{ pool: ['小明', ...], lastRoster: [{seat, name}, ...] }`
- 姓名池去重，最近使用优先，上限 50。
- 与对局存档生命周期**完全独立** —— 姓名需跨局留存，这正是该功能的意义。
- `清空已存姓名` 同时清空 `pool` 与 `lastRoster`。
- **隐私**：所有数据仅存于本机 localStorage，永不离开设备。README 与设置页均须明示。

### 11.3 版本化

`GameState.version` 与代码内常量比对，不匹配时**丢弃存档**并提示，而非在陈旧结构上崩溃。迭代期的廉价保险。

---

## 12. 界面与布局

### 12.1 局内布局

```
┌─────────────────────────────┐
│ 第 2 晚   狼3·神2·民4        │ ← 固定：阶段 + 阵营计数
│ 02:30 ▶ ± ↶ ☰  长按结束      │ ← 固定：计时器 + 撤销 + 日志 + 结束
├─────────────────────────────┤
│ ⚠ 同守同救：该玩家本回合仍死亡 │ ← 常驻警告横幅（如有）
├─────────────────────────────┤
│ 女巫                         │ ← 固定：当前步骤 + 口播提示 + 操作
│ 今晚死亡：3号                 │
│ [使用解药][使用毒药][不使用]   │
├─────────────────────────────┤
│ ┌──┐┌──┐┌──┐                │
│ │1 ││2 ││3 │                │ ← 唯一可滚动区域：玩家网格
│ └──┘└──┘└──┘                │
└─────────────────────────────┘
```

**顶部控制区与步骤区固定**，仅玩家网格可滚动 —— 法官永远不会因滚动而失去计时器与当前步骤。

### 12.2 自适应密度

| 人数 | 列数 | 卡片内容 |
|---|---|---|
| ≤ 9 | 3 列 | 座位号 + 图标 + 姓名 + 身份 + 技能标签 |
| 10–16 | 4 列 | 座位号 + 图标 + 姓名 + 身份 |
| 17–20 | 5 列 | 座位号 + 图标 + 截断姓名（身份与标签点击展开） |

- **阵亡卡片折叠**至约 42px，仅显示座位号 + 身份 + 死因标签。死亡随对局推进而累积，因此空间压力随时间递减。
- **零滚动保证：≤ 12 人**。13–20 人允许网格区域滚动，但顶部控制区始终固定。此保证写入规格，不做超出能力的承诺。

### 12.3 屏幕常亮

对局进行中申请 **Screen Wake Lock**（iOS Safari 16.4+ / Chrome 均支持），`长按结束` 时释放。不支持的环境静默失败。

### 12.4 视觉设计

沿用 Modernist 设计系统的**令牌架构与扁平风格**：0 圆角、2px 分隔线、标签左对齐、单一强调红、100–900 色阶。

**两处必须修正**：

1. **移除 Google Fonts。** Archivo 无 CJK 字形覆盖 —— 本应用界面几乎全为中文，所有汉字本就回落到系统字体。为少量拉丁字形付出阻塞渲染的外部请求毫无意义，且引入外部网络依赖。改用系统字体栈：
   ```css
   font-family: system-ui, -apple-system, "PingFang SC", "Hiragino Sans GB", "Microsoft YaHei", sans-serif;
   ```
2. **新增暗色主题。** 狼人杀在昏暗环境中进行，浅色主题（`#f3f2f2`）在夜间刺眼且会向玩家泄露光线。
   - `:root` 定义完整浅色令牌；
   - `@media (prefers-color-scheme: dark)` 内以 `:root:not([data-theme="light"])` 保护地覆写；
   - `:root[data-theme="dark"]` 再次覆写，使手动切换在两个方向都生效；
   - **局内界面默认暗色**，保留强调红（在暗底上表现良好，且延续 Modernist 识别度）。

CSS **复制进本项目的 `style.css`**，不引用 `_ds` 包 —— 那是 Claude Design 的基础设施，不属于本项目。

### 12.5 图标

19 个**手绘内联 SVG**，以 sprite（`<symbol>` + `<use>`）方式引入。

- Lucide 视觉语言：24×24 viewBox、1.5–2px 描边、`currentColor`、无填充。
- **狼族以「基础狼形 + 修饰符」区分**（原型中 6 个狼角色共用同一图标，恰恰使驱动死亡触发逻辑的角色无法辨识）：
  - 普通狼人 —— 基础狼形
  - 狼王 —— + 王冠
  - 白狼王 —— + 王冠 + 爆裂星芒
  - 狼美人 —— + 唇形 / 心形
  - 隐狼 —— 虚线 / 残缺轮廓
  - 机械狼 —— + 齿轮 / 螺栓
- 阵营**不以颜色区分**（Modernist 为单色方案）—— 依靠图形本身与卡片下方的身份文字。

### 12.6 无障碍

- 点击目标 ≥ 44px。
- 图标按钮附 `aria-label`。
- `:focus-visible` 强调色焦点环（设计系统已提供）。
- 不追求完整屏幕阅读器支持。

---

## 13. PWA

### 13.1 manifest.json

```json
{
  "name": "狼人杀法官助手",
  "short_name": "法官助手",
  "start_url": "./",
  "scope": "./",
  "display": "standalone",
  "orientation": "portrait",
  "background_color": "#1a1918",
  "theme_color": "#1a1918",
  "icons": [ ... ]
}
```

- `display: standalone` 移除 Safari 的地址栏与底部工具栏，**回收约 120px 垂直空间**，直接服务于 §12.2 的零滚动目标。
- `orientation: portrait` 由操作系统强制竖屏，恰好落实「只需适配竖屏浏览方式」的需求。

### 13.2 Service Worker

- **版本化 cache-first**：缓存名 `wolf-v1`，每次发布递增。
- `install` 时**预缓存全部资源**（总计约 50KB，全部本地，无运行时 fetch 策略需求）。
- `activate` 时删除所有非当前版本的缓存。
- `skipWaiting()` + `clients.claim()`，使更新在下次启动即生效，而非长期滞留旧版本。

> **陈旧缓存是 Service Worker 的经典陷阱**。上述版本化纪律是必须遵守的约定，而非可选优化。

### 13.3 离线价值

狼人杀常在地下室、酒吧、KTV、车上、火车上进行 —— 信号恶劣。法官打开应用却遇到网络错误即为彻底失败。由于已移除全部外部依赖，离线能力近乎免费获得。

---

## 14. 计时器

### 14.1 实现要求

- **以截止时间戳（`endsAt`）存储**，每次渲染时推算剩余时间。
  > 原型以 `setInterval` 递减整数，浏览器在后台标签页会节流定时器，导致计时静默变慢或停滞。必须修正。
- 暂停时保存 `pausedRemaining`，恢复时重算 `endsAt`。
- 随对局存档持久化，可跨刷新与后台切换。

### 14.2 两种模式

**自由计时** —— 全局倒计时。快捷档位芯片：30s / 60s / 90s / 120s / 180s，以及 ±10s 微调。

**发言计时** —— 白天使用。
- 由随机首发言抽取（§15）或法官手动指定起始座位与方向。
- 高亮当前发言者卡片。
- `下一位` 自动跳至下一**存活**座位（跳过死者），`上一位` 可回退。
- 这是应用在白天阶段的核心价值 —— 原型的白天几乎是空白阶段。

### 14.3 到时警报

归零时触发 §10 的警报通道：强视觉闪烁 + 可用时震动 + 可选提示音。非阻断。

---

## 15. 每日随机首发言

替代被移除的警长决定发言顺序的职能。

- **设置开关** `randomFirstSpeaker`，位于高级规则区，**默认开启**。
- **天亮结算确认后自动抽取**（此时死者已排除）。
- 同时随机**座位与方向**，内联横幅显示：`本轮发言：5号 开始 · 顺时针`。
- `重新抽取` 按钮。
- **直接驱动发言计时** —— `开始发言` 即以该座位与方向起步。
- 以 `system` 类型写入日志，使日志记录每日发言顺序。
- 使用 `crypto.getRandomValues`（随机分配身份同样如此），消除对随机性可预测的质疑。

---

## 16. 日志

### 16.1 结构化事件

日志条目为**类型化记录**（§3.4），而非预先拼接的字符串。类型：`skill` / `death` / `system` / `note` / `warning`。这使得筛选、导出与渲染成为可能。

### 16.2 浏览方式

- **按阶段分组折叠** —— `第1夜` / `第1天` / `第2夜` …… 当前阶段展开，历史阶段折叠，点击展开。直接落实「可以浏览第几天或者第几夜发生过什么」。
- **组内按时间正序**（事件读起来像故事），**组间倒序**（当前阶段位于顶部，无需滚动）。
- **筛选芯片**：`全部` / `死亡` / `技能` / `备注`。一键查看完整死亡史 —— 局内最常被问到的问题。
- **法官备注**：底部输入框添加，条目可删除（点击 → 删除，先执行 + 撤销条）。系统与技能条目**不可编辑**，它们是审计轨迹。
- 日志为**整页替换式屏幕**，非覆盖层，带 `返回` 按钮。
- 供战报页的 `复制日志文本` 导出。

---

## 17. 胜负判定

- **阵营计数条**常驻顶部：`狼 3 · 神 2 · 民 4`。仅统计**身份已知且存活**的玩家；存在未知身份座位时附加标记，提示数字不可尽信。
- **不做自动胜负判定。** 依据不完整的身份信息宣告胜利会产生误判，比沉默更糟；且宣告胜负是法官不可让渡的职权。此决策同时使我们无需引入「屠边 / 屠城」开关。
- 胜方由法官在战报页点选。

---

## 18. 测试

`engine.js` 的天亮结算与触发链写为**纯函数**，`test.html` 以断言方式覆盖已知场景。

**必测场景**（结算是 bug 会静默污染实时对局的地方）：

1. 狼刀 + 守卫守护同一人 → 无人死亡
2. 狼刀 + 女巫解药同一人 → 无人死亡
3. **同守同救** → 依开关判定（默认死亡）+ 触发警告
4. 女巫毒杀猎人 → 猎人**不入**开枪队列（默认开关）
5. 情侣连锁：狼刀情侣 A → B 殉情
6. 情侣 + 猎人连锁：猎人被投 → 开枪击中情侣 A → B 殉情 → B 为狼王 → 狼王入队
7. 狼美人死亡 → 被魅惑者殉情
8. 魔术师交换 → 狼刀指向被重定向
9. 守卫连守违规 → 触发警告但不阻断
10. 熊咆哮：邻座为狼 / 非狼 / 死者跨越 / 身份未知三态
11. 连锁收敛：构造循环情侣链，验证不死循环

其余部分依靠手工测试。

---

## 19. 需求追溯

| 原始需求 | 落实章节 | 状态 |
|---|---|---|
| 功1 选择人数与角色牌 | §4.1 Step 1 | ✅ 6–20人，19 角色，2 预设 |
| 功2 记录玩家名字 | §4.1 Step 2、§11.2 | ✅ 选填 + 姓名池持久化 + 拖拽排序 |
| 功3 流程引导 | §4.2、§4.3 | ✅ 夜晚步骤引导 + 白天阶段编排 |
| 功4 标记技能使用状况 | §6 | ✅ 自动消耗 + 卡片标签 + 驱动流程 |
| 功5 计时功能 | §14 | ✅ 自由计时 + 发言计时 |
| 功6 标记出局与原因 | §5.2、§8.3 | ✅ 长按 + 死因芯片 + 引擎推算 |
| 功7 选择夜晚轮次 | §4.1 Step 3 | ✅ 拖拽排序 + 恢复默认 |
| 功8 同守同救高亮 + 微震动 | §10 | ⚠️ **视觉高亮完整实现；震动为渐进增强，iOS 不支持** |
| 功9 局内日志按天/夜浏览 | §16 | ✅ 分组折叠 + 筛选 + 导出 |
| 设1 避免过多滚动 | §12.1、§12.2 | ✅ ≤12人零滚动保证；13–20人网格可滚动 |
| 设2 长按标记阵亡 | §8.3 | ✅ 550ms 长按 → 内联死因芯片 |
| 设3 阵亡状态可折叠 | §12.2 | ✅ 折叠至 42px，显示身份 + 死因 |
| 设4 不能有任何弹窗 | §8.1、§8.2 | ✅ 全部确认经由长按守护或撤销条 |
| 设5 简易设计风格 | §12.4 | ✅ Modernist 令牌 + 暗色主题 |
| 新1 姓名持久化 + 清空 | §11.2 | ✅ 姓名池 + 沿用上次名单 + 长按清空 |
| 新2 拖拽排序玩家 | §4.1 Step 2 | ✅ 把手拖拽，Pointer Events |
| 新3 每日随机首发言 | §15 | ✅ 默认开启，驱动发言计时 |

---

## 20. 已知限制与 v2 储备

**已知限制**
- 微震动在 iOS 上不可用（平台限制，无解）。
- 13–20 人时局内网格需要滚动。
- 魔术师与机械狼的语义细节交由法官人工裁定，引擎不做完整推演。
- 无自动胜负判定。
- 单槽存档：开始新局即覆盖上一局（战报页的日志复制是唯一留存途径）。

**v2 储备（架构已预留，无需重构）**
- 警长：徽标位与移交/撕毁触发可直接接入 §5.4 的触发队列与卡片徽标系统。
- 私密查看身份：`roleId` 数据模型已完备。
- 投票计票：日志的类型化事件结构可直接承载。
- 多局归档：存储层加一个 key 命名空间即可。
